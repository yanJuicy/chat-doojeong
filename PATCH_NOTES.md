# 패치노트

문제 → 원인 → 해결 순서로, 한 번에 처리한 작업 단위(볼륨)마다 하나씩 기록한다.
나중에 "이 프로젝트가 어떤 과정을 거쳐 지금 형태가 됐는지" 되짚어보는 용도.

---

## 2026-08-07 (5) — 워커 중복 처리 방지 + 실패 복구

**문제**: 워커(추출/청킹/임베딩)가 동시에 두 번 실행되면 같은 문서를 중복 처리할 수 있었다.
또 임베딩 워커는 배치 하나가 실패하면 커밋을 맨 마지막에 한 번만 해서, **그 전에 이미 성공한
배치까지 통째로 롤백되어 날아가는** 진짜 버그가 있었다. 실패한 문서는 재시도 수단이 없어서
`FAILED` 상태로 영원히 멈춰있었다.

**해결**:
- 세 워커의 조회 쿼리에 `FOR UPDATE SKIP LOCKED`를 걸어서, 동시에 실행돼도 같은 행을 두 워커가
  같이 집어가지 못하게 함 (PostgreSQL 기준 실제로 잠김, SQLite는 무시하지만 테스트 용도라 무방).
- `Document.retry_count`, `DocumentChunk.embed_retry_count` 컬럼 추가. 실패해도 바로 FAILED로
  확정하지 않고, 설정된 횟수(`WORKER_MAX_RETRIES`, 기본 3)까지는 상태를 그대로 둬서 다음 워커
  실행 때 자동으로 재시도되게 함.
- 임베딩 워커는 **배치마다 커밋**하도록 재구조화. 배치 하나가 실패해도 그 전 배치의 진행상황은
  지켜지고, 실패한 배치만 재시도 카운트를 올리고 계속 진행함.
- `POST /api/documents/{id}/retry` 엔드포인트 추가 (재시도 상한을 넘겨 완전히 멈춘 문서를
  수동으로 다시 살릴 때 사용). 콘솔에 `failed` 상태 문서 옆에 재시도 버튼도 붙임.

**검증**: SQLite로 실제 DB를 만들어 (1) 배치 하나가 의도적으로 실패해도 이전 배치는
`embedded=True`로 남아있는지, (2) 재시도 API가 추출전실패/청킹실패/임베딩방치청크 세 가지
케이스 모두 정확히 되돌리는지 확인.

---

## 2026-08-07 (4) — Alembic 도입 (DB 스키마 관리)

**문제**: 지금까지 `Base.metadata.create_all()`만 쓰고 있었는데, 이건 "테이블이 없으면
만들어주는" 것만 하고 **이미 있는 테이블에 컬럼을 추가하는 건 못 한다**. 그래서 컬럼을 하나
추가할 때마다 매번 `docker exec postgres psql ... ALTER TABLE`을 수동으로 쳐야 했다
(이 세션에서만 6번 넘게 반복됨).

**해결**: Alembic 도입. `migrations/env.py`가 `app/config.py`의 `POSTGRES_DSN`을 그대로
재사용하도록 연결해서 별도 설정 없이 바로 쓸 수 있게 함. 지금까지 쌓인 모든 컬럼 변경
(`file_hash`, `category`, `table_confidence`, `retry_count` 등)을 담은 최초 마이그레이션
(`0001_initial_schema.py`) 작성. `main.py`의 `create_all()` 호출은 제거하고, 서버 시작 시
"마이그레이션을 먼저 실행하라"는 로그로 교체.

**검증**: 마이그레이션 스크립트를 실제로 실행해서 만든 스키마와, `models.py`를
`create_all()`로 직접 만든 스키마를 테이블·컬럼 단위로 전부 비교해서 완전히 일치함을 확인.

---

## 2026-08-07 (3) — 매뉴얼/가이드류 문서를 위한 구조 기반 청킹

**문제**: 의미기반 청킹(인접 문장 유사도만 봄)은 매뉴얼처럼 "1. 설치 > 1.2 설치 절차"식으로
구조가 뚜렷한 문서에서, 4단계짜리 절차를 중간에서 잘라버릴 수 있는 구조적 한계가 있었다.

**해결**: `StructuredChunker` 신규 구현. 제목처럼 보이는 줄(마크다운 `#`, 번호매기기 `1.`,
`1.1` 등)을 찾아 섹션 경계로 우선 삼되, `"1. 프로그램을 종료한다."`처럼 완결된 문장(절차
단계)은 제목으로 오인하지 않도록 길이+문장종결어미로 구분. 제목이 2개 미만인 구조 없는
문서는 기존 의미기반 청킹(`SemanticChunker`)으로 자동 위임. 너무 작은 섹션은 **같은 상위
섹션 아래일 때만** 병합(다른 주제 섹션과 무분별하게 합쳐서 라벨이 뒤섞이는 걸 방지). 청크
본문 앞에 `[섹션: 1. 설치 > 1.2 설치 절차]`처럼 소속 경로를 붙여 검색 결과에서 바로 확인
가능하게 함. 기본 청커로 교체 적용.

**검증**: 실제 설치 가이드 예시 텍스트로 4단계 절차가 하나의 청크에 안 끊기고 유지되는지,
서로 다른 섹션끼리 라벨이 안 섞이는지, 구조 없는 문서는 정상적으로 의미기반 청킹에
위임되는지 확인.

---

## 2026-08-07 (2) — 청킹 단계의 이중 임베딩 낭비 제거

> 2026-08-12 후속 정리: 청크 텍스트에 문서 제목이 붙는 현재 파이프라인에서는 문장 벡터를
> 그대로 재사용할 수 없어 이 최적화가 실제로 사용되지 않았다. 해당 필드와 저장 경로는 제거했다.

**문제**: 청킹 시 경계를 정하려고 문장마다 임베딩을 계산하는데, 그 청크가 나중에 저장될 때
`embedding_worker`가 저장용으로 **또 한 번** 임베딩을 계산하고 있었다. 청크가 문장 하나로만
이루어지는 경우(짧은 문단, 목록 항목 등에서 흔함)엔 이 둘이 완전히 같은 계산이라 순수 낭비.

**해결**: `Chunk`/`DocumentChunk`에 `precomputed_dense_vector` 필드 추가. 청킹 단계에서 문장이
1개짜리 청크가 만들어질 때, 경계 판단용으로 이미 계산해둔 그 문장의 임베딩을 그대로 실어서
DB에 저장(JSON 직렬화). 임베딩 워커는 이 값이 있으면 dense 임베딩 재계산을 건너뛰고 그대로
재사용 (sparse는 청킹 단계에서 안 만들어지므로 항상 새로 계산).

**검증**: 문장 3개짜리(전부 서로 다른 주제라 각각 별도 청크가 되는) 문서로, 청킹 단계에서
`embed_documents`가 1번(배치로 3개 문장) 호출되고, 이후 임베딩 워커 단계에서는 **0번**
호출되는 것을 실제로 확인 (전부 재사용됨).

---

## 2026-08-07 (1) — 실제 토크나이저 기반 청크 길이 계산

**문제**: 청크 최대 길이를 "글자수 = 토큰수 × 2"로 근사하고 있었다. 실제 토큰화는 한국어/영어
비율, 숫자, 특수문자에 따라 이 근사와 꽤 어긋날 수 있어서, 설정한 `CHUNK_MAX_TOKENS`가
실제로는 정확히 안 지켜지고 있었다.

**해결**: `BaseEmbeddingProvider`에 `count_tokens(text) -> int` 추상 메서드 추가.
`BgeM3EmbeddingProvider`는 bge-m3가 실제로 쓰는 HuggingFace 토크나이저
(`self._model.tokenizer`)로 진짜 토큰 개수를 센다 (인코딩 없이 토크나이즈만 하므로 빠름).
`SemanticChunker`의 청킹 경계 판단 로직을 글자수 비교에서 실제 토큰수 비교로 교체.

**검증**: 글자수는 많지만 토큰수는 고정(3토큰)인 가짜 문장들로, 청킹 경계가 실제 토큰수
기준(최대 10토큰 → 문장 3개씩)으로 정확히 결정되는 것을 확인.
# 2026-08-09 — 품질 게이트·검색·워커 안정성 개선

- 저품질 OCR 점수화 및 `needs_review` 차단 상태 추가
- PPStructure/경량 OCR 자동 폴백 후 더 좋은 결과 선택
- PDF 페이지 마커와 청크 `page_number` 보존
- 질문에 명시된 기존 라벨을 PostgreSQL에서 문서 ID로 해석해 Qdrant 검색 범위 제한
- Qdrant payload에 라벨·파일명·카테고리·파이프라인 버전 저장
- 검색 시 PostgreSQL에서 READY가 아닌 문서의 오래된 Qdrant 포인트 제외
- BGE-M3 dense+sparse 한 번에 생성
- 워커 claim 배치 제한 및 멈춘 extraction 자동 회수
- 문서 변경 시 corpus revision이 달라져 동일질문 캐시 자동 무효화
- 평가 API에 정답 순위, MRR, 필수어 포함률 추가
- 기존 라벨·원본을 보존하고 잘못된 OCR부터 다시 만드는 `/reextract` API 추가
- 기본 Compose 실행에서 app 제외(`container-app` profile)
- Paddle/Numpy/greenlet/ujson/python-multipart 의존성 명시

## 2026-08-09 — PDF 페이지 프로파일링 v3

- 단순 `네이티브 텍스트 20자` 기준 대신 텍스트 품질·폰트 깨짐·이미지 합집합 점유율을 함께 판정
- 페이지별 `digital / ocr / mixed` 분류와 판정 이유를 추출 품질 진단 JSON에 저장
- 제목만 텍스트이고 본문이 큰 이미지인 혼합 페이지는 전체 페이지 한글 OCR을 병행
- 혼합 페이지의 네이티브/OCR 결과는 풍부한 쪽을 선택하거나 유사 문장을 제거해 병합
- 모든 페이지 결과로 문서 추출 방식을 `pdf_text / pdf_ocr / pdf_mixed`로 기록
- OCR 없이 빠르게 분류만 수행하는 `scripts/audit_pdf_pages.py` 추가

## 2026-08-09 — PDF OCR 캐시 격리 v4

- OCR 캐시 키에 언어·파이프라인 버전을 추가해 과거 영문/중문 OCR 캐시 재사용 차단
- 한국어 문서에서 한자 치환 비율이 높은 결과의 품질 페널티를 강화해 임베딩 진입 차단
- 등록 문서 API와 `{document_id}_원본명.pdf` 규칙을 연결한 101개 PDF 감사 모드 추가

## 2026-08-09 — 디지털 PDF 불필요 이미지 I/O 제거 v5

- 이미지 캡셔닝이 꺼진 상태에서는 PDF 삽입 이미지를 별도 파일로 저장하지 않음
- 원본 PDF는 그대로 보존하므로 캡셔닝을 켠 뒤 재처리하면 이미지 추출 가능
- 정상 텍스트가 대부분인 수백 페이지 매뉴얼의 재처리 시간을 크게 단축
