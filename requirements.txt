# 앱 공통 의존성
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.0.0
pydantic-settings>=2.0.0
sqlalchemy[asyncio]>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0
greenlet>=3.0.0
qdrant-client>=1.9.0
httpx>=0.27.0
kiwipiepy>=0.17.0
langdetect>=1.0.9

# 임베딩/리랭커 (bge-m3, bge-reranker-v2-m3)
FlagEmbedding>=1.4.0
torch>=2.2.0
numpy>=1.24,<2.4

# PDF 텍스트 추출 (디지털 텍스트 페이지용)
pymupdf>=1.24.0

# Word(.docx) 문서 추출
python-docx>=1.1.0

# 웹 크롤링/HTML 파싱 (인터넷 되는 환경에서만 사용, 폐쇄망 서버에서는 호출 안 함)
beautifulsoup4>=4.12.0
requests>=2.31.0

# 표 추출 모듈 의존성은 app/services/table_extraction/requirements.txt 참고
